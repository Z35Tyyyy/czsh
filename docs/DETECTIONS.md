# Detection Catalog

> Generated by `tools/generate_catalog.py` — do not edit by hand.

**15 detections** covering **17 ATT&CK techniques**, backed by **61 offline test assertions**.

## Execution

| Rule | ATT&CK | Severity | Tests | Summary |
|------|--------|----------|-------|---------|
| `proc_creation_office_spawns_shell` | T1059 | high | 4 | Detects a Microsoft Office application (Word, Excel, PowerPoint, Outlook) spawning a command interpreter or script host. |
| `proc_creation_powershell_encoded` | T1059.001 | high | 4 | Detects PowerShell (powershell.exe / pwsh.exe) invoked with an encoded-command flag (-EncodedCommand / -enc / -ec). |
| `proc_creation_wmic_process_call_create` | T1047 | medium | 4 | Detects use of WMIC to spawn a new process via "process call create". |

## Persistence

| Rule | ATT&CK | Severity | Tests | Summary |
|------|--------|----------|-------|---------|
| `proc_creation_local_account_manipulation` | T1136.001, T1098 | medium | 4 | Detects creation of a new local account (net user /add) or the addition of an account to the local Administrators group (net localgroup administrators /add). |
| `registry_run_key_suspicious` | T1547.001 | high | 4 | Detects a value written to a CurrentVersion\Run or \RunOnce autostart key whose data points at a script host, LOLBIN, or a user-writable staging directory (AppData/Temp/ProgramData). |
| `proc_creation_scheduled_task_create` | T1053.005 | medium | 4 | Detects creation of a scheduled task (schtasks /create) whose action runs a script interpreter, an encoded command, a binary staged in a user-writable directory, or runs as SYSTEM. |

## Defense Evasion

| Rule | ATT&CK | Severity | Tests | Summary |
|------|--------|----------|-------|---------|
| `proc_creation_disable_defender` | T1562.001 | high | 5 | Detects attempts to weaken Microsoft Defender: disabling real-time/behavior monitoring via Set-MpPreference, stopping the WinDefend service, or adding a broad ExclusionPath so future payloads are ignored. |
| `proc_creation_mshta_remote` | T1218.005 | high | 4 | Detects mshta.exe (a signed Microsoft binary) being used to proxy execution of a remote payload (http/https) or inline script (javascript:/vbscript:). |
| `proc_creation_clear_eventlog` | T1070.001 | high | 4 | Detects clearing of Windows event logs via wevtutil (cl/clear-log) or the PowerShell Clear-EventLog / Remove-EventLog cmdlets. |

## Credential Access

| Rule | ATT&CK | Severity | Tests | Summary |
|------|--------|----------|-------|---------|
| `proc_creation_ntdsutil_dump` | T1003.003 | high | 4 | Detects use of ntdsutil to create an Install-From-Media (IFM) copy of the NTDS.dit Active Directory database. |
| `proc_creation_lsass_dump` | T1003.001 | high | 4 | Detects attempts to dump the memory of LSASS, the process that holds cached credentials and secrets. |
| `proc_creation_reg_save_hive` | T1003.002 | high | 4 | Detects use of reg.exe to save the SAM, SYSTEM, or SECURITY registry hives to disk. |

## Discovery

| Rule | ATT&CK | Severity | Tests | Summary |
|------|--------|----------|-------|---------|
| `proc_creation_ad_recon` | T1087.002, T1482 | medium | 4 | Detects domain reconnaissance performed with living-off-the-land tools: net group/user queries against the domain, nltest trust and DC enumeration, and dsquery. |

## Command And Control

| Rule | ATT&CK | Severity | Tests | Summary |
|------|--------|----------|-------|---------|
| `proc_creation_certutil_download` | T1105 | high | 4 | Detects certutil.exe being abused to download a remote file (urlcache / verifyctl with an http/https/ftp URL). |

## Impact

| Rule | ATT&CK | Severity | Tests | Summary |
|------|--------|----------|-------|---------|
| `proc_creation_shadowcopy_delete` | T1490 | critical | 4 | Detects deletion of Volume Shadow Copies and tampering with Windows recovery, via vssadmin, wmic shadowcopy, wbadmin, or bcdedit. |

